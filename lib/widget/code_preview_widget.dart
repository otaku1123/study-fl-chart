import 'package:flutter/material.dart';

class CodePreviewWidget extends StatelessWidget {
  const CodePreviewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('コードの表示');
  }
}
