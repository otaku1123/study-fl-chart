import 'package:flutter/material.dart';

class ChartAreaWidget extends StatelessWidget {
  const ChartAreaWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('チャートエリア');
  }
}
